 <h3>View Creation</h3>
 <div class="alert alert-info">
 This is the simplest view creation where the columns of each dataset will be joined by name. If any names overlap they will be combined based on the timestamp if one exists. The primary dataset values will be retained in case of collisions.
 </div>
 <%= form_tag({action: :create_view}) do %>  
  <div class="form-group">
      <label for="view_name">View Name: </label><input type="text" name="view_name">
  </div>
  <div class="form-group">
      <label for="dataset_name">Primary Dataset: </label>
      <select name="dataset_name">
        <%@datasets.each do |ds|%>
        <option><%= ds.dataset_name %></option>
        <%end%>
      </select>
  </div>
  <div class="form-group">
      <label for="join_dataset_name">Joined Dataset: </label>
      <select name="join_dataset_name">
        <%@datasets.each do |ds|%>
          <option><%= ds.dataset_name %></option>
        <%end%>
      </select>
  </div>
  <div class="form-group">
      <input type="submit" text="create view">
  </div>
 <%end%>